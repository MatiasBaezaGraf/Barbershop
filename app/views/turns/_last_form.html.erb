  <%= form_with(model: turn) do |form| %>
    <% if turn.errors.any? %>
      <div id="error">
        <h2><%= pluralize(turn.errors.count, "error") %> prohibited this turn from being saved:</h2>

        <ul>
          <% turn.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

  <h3 class = "paragraph">Elija su horario</h3>

  <% Turn.posibles(@occupied, @times, @turn.time, @turn.count_hours, @turn) %>

  <% puts "ESTOS VALEN", @times%>
  <% puts "ESTE VIENE YA", @turn.time%>

  <div class="field">
    <%= form.select(:time, @times.collect { |t| [ t.strftime("%H:%M"), t ] }, {},{ :class => 'admin_select' })%>
  </div>



  <br>
  <br>
  <br>

    <div class="actions">
      <%= form.submit "INGRESAR", class: 'but-login' %>
    </div>
<% end %>